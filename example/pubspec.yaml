name: glint_capture_example
description: Example app demonstrating glint_capture screenshot rules.
publish_to: 'none'

environment:
  sdk: ^3.8.0
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  glint_capture:
    path: ../

flutter:
  uses-material-design: true
